name: Mirror to fgcz/new_SUSHI_2025 (disabled)
on:
  workflow_dispatch:

jobs:
  mirror:
    if: false  # temporarily disabled per request (no private key to use)
    runs-on: ubuntu-latest
    steps:
      - name: Skip mirroring
        run: echo "Mirroring is temporarily disabled"

